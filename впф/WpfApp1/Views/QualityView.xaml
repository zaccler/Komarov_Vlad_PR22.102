<UserControl x:Class="WpfApp1.Views.QualityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Обновить" Width="90" Margin="0,0,6,0" Command="{Binding LoadCommand}"/>
            <Button Content="Добавить" Width="90" Margin="0,0,6,0" Command="{Binding AddCommand}"/>
            <Button Content="Удалить" Width="90" Margin="0,0,6,0" Command="{Binding DeleteCommand}"/>
            <Button Content="Сохранить" Width="110" Command="{Binding SaveCommand}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding QualityDefectId}" Width="60"/>
                    <DataGridTextColumn Header="StageId" Binding="{Binding ProductionStageId}" Width="80"/>
                    <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90"/>
                    <DataGridTextColumn Header="Decision" Binding="{Binding Decision}" Width="90"/>
                    <DataGridTextColumn Header="Desc" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Карточка дефекта" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="ProductionStageId"/><TextBox Text="{Binding Selected.ProductionStageId}" Margin="0,4,0,10"/>
                <TextBlock Text="Severity (low/medium/high)"/><TextBox Text="{Binding Selected.Severity}" Margin="0,4,0,10"/>
                <TextBlock Text="Decision (repair/scrap/accept)"/><TextBox Text="{Binding Selected.Decision}" Margin="0,4,0,10"/>
                <TextBlock Text="QtyAffected"/><TextBox Text="{Binding Selected.QtyAffected}" Margin="0,4,0,10"/>
                <TextBlock Text="Description"/><TextBox Text="{Binding Selected.Description}" Height="80" AcceptsReturn="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
