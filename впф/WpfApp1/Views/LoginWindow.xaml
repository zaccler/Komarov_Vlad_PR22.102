<Window x:Class="WpfApp1.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="250" Width="360" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин" />
        <TextBox Grid.Row="1" Margin="0,6,0,10"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Пароль" />
        <PasswordBox x:Name="Pwd" Grid.Row="3" Margin="0,6,0,10"
                     PasswordChanged="Pwd_PasswordChanged"/>

        <StackPanel Grid.Row="4">
            <Button Content="Войти" Height="34"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            <TextBlock Margin="0,10,0,0" Foreground="Red"
                       Text="{Binding Error}" />
        </StackPanel>
    </Grid>
</Window>
